services:
  - type: web
    name: habit-tracker-api
    env: node
    plan: free
    region: oregon
    buildCommand: cd apps/backend && npm install && npx prisma generate
    startCommand: cd apps/backend && node simple-server.js
    healthCheckPath: /api/health
    # Environment variables (will be set in Render dashboard)
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: habit-tracker-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  - name: habit-tracker-db
    plan: free
